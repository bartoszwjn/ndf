# This file is a template for creating test cases,
# it's intentionally not registered as a test case.
[command]
args = []

[repo_contents.git]

[[repo_contents.git.commits]]
message = "commit 1"
changes."default.nix".write = """
let
  drv = import ./drv.nix;
in
{
  TODO
}
"""
changes."flake.nix".write = """
{
  outputs =
    inputs:
    let
      drv = import ./drv.nix;
    in
    {
      packages.@SYSTEM@ = {
        TODO
      };
    };
}
"""

[outputs]
exit_code = 42
stdout = ""
stderr = ""
